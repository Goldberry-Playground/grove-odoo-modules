# Pure-Python service helpers, free of Odoo imports so they can be unit-tested
# by file path in CI (see grove_headless/tests/conftest.py). Wired into the Odoo
# models by the Fetch-button handler in a follow-up (section A dependency).
