# ownership.yml — repo partition + review-routing map for the AgenticOS
# group-coding flow (GOL-152). See docs/ownership-schema.md for full field
# definitions. Validate with: python3 scripts/lint-ownership.py
#
#   version | repo | defaults.review | partitions[{name, paths, owner, review, wave}]
version: 1
repo: grove-odoo-modules

defaults:
  review: [engineering-ada]

partitions:
  - name: module-grove-headless
    paths:
      - grove_headless/**
    owner: engineering-ada
    review: [devops-terra]
    wave: 1
  - name: module-grove-support
    paths:
      - grove_support/**
    owner: engineering-ada
    review: [devops-terra]
    wave: 1
  - name: module-web-dark-mode
    paths:
      - web_dark_mode/**
    owner: frontend-iris
    review: [engineering-ada]
    wave: 1
  - name: scripts
    paths:
      - scripts/**
    owner: engineering-ada
    review: [devops-terra]
    wave: 1
  - name: docs
    paths:
      - docs/**
    owner: engineering-ada
    review: [devops-terra]
    wave: 3
